in setup.json everything can come from datasource
    prefix stuff with :: if it should be looked up in datastore
widgets
    progres up bar
    bit lamp
    pie charts
    gauge
    time widget
color setup

command line arguments

test client

wicked demo config
    random generator or something from /proc etc.

=== first release ===
